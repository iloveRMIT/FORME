document.addEventListener('DOMContentLoaded',()=>{const root=qs('#featuredProducts');if(root){root.innerHTML=inventory.slice(0,4).map(productCard).join('');bindProductCards(root);}const heroLead=qs('.hero .lead');if(heroLead)heroLead.textContent='Start with your wardrobe. Add only what the occasion needs.';const feature=root?.closest('.section');if(feature){const wardrobePreview=getWardrobe().slice(0,3);feature.insertAdjacentHTML('beforebegin',`<section class="section wardrobe-home"><div class="container"><div class="split-heading"><div><p class="eyebrow">YOUR CLOSET, BUILT IN</p><h2>Save what you own.<br><em>Style it again later.</em></h2></div><a class="button outline" href="wardrobe.html">Open my wardrobe</a></div><div class="wardrobe-grid wardrobe-preview">${(wardrobePreview.length?wardrobePreview.map(item=>`<article class="wardrobe-card"><span class="wardrobe-image-frame"><img src="${item.image}" alt="${item.name}"></span><span class="card-badge">OWNED</span><b>${item.name}</b></article>`):['Trousers','Blazer','Shoes'].map((name,index)=>`<article class="wardrobe-card"><span class="wardrobe-image-frame"><img src="${productImage(inventory[[6,0,7][index]])}" alt="${name} placeholder"></span><span class="card-badge">OWNED</span><b>${name}</b></article>`)).join('')}</div></div></section>`);}});
