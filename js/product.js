document.addEventListener('DOMContentLoaded',()=>{const item=byId(query('id'));const root=qs('#productPage');if(!item){root.innerHTML='<div class="state-card"><p class="eyebrow">FORME CAPSULE</p><h1>Product not found.</h1><p>This capsule piece may have moved.</p><a class="button dark" href="rent.html">Return to capsule</a></div>';return;}document.title=`${item.name} — FORME`;root.innerHTML=`<div class="breadcrumbs"><a href="index.html">Home</a><span>/</span><a href="rent.html">Rent</a><span>/</span>${item.category}<span>/</span>${item.name}</div><div class="product-main"><div><div class="gallery-main"><img src="${productImage(item)}" alt="${item.name} placeholder — replace image"></div><div class="thumbnails">${[1,2,3].map(()=>`<img src="${productImage(item)}" alt="${item.name} gallery placeholder">`).join('')}</div></div><section class="product-info"><p class="eyebrow">${item.collection.toUpperCase()} / ${item.category.toUpperCase()}</p><h1>${item.name}</h1><p class="status">● Available</p><p class="product-price">${vnd(item.price)} <small>/ 3 days</small></p><p>${item.desc}</p><p><b>Colour:</b> ${item.color}</p><label class="form-label">Select size</label><div class="size-buttons">${item.sizes.map((s,i)=>`<button class="${i===0?'active':''}" data-size="${s}">${s}</button>`).join('')}</div><a class="size-guide-link" href="faq.html">Size guide</a><label class="form-label">Event date</label><input id="productDate" type="date"><div class="date-preview" id="datePreview">Choose an event date for your delivery window.</div><button id="addProduct" class="button dark full">Add to rental bag</button><button id="saveProduct" class="button outline full">${getSaved().includes(item.id)?'Saved item':'Save item'}</button><a class="button outline full" href="style.html?item=${item.id}">Style this item</a><p class="fine">Already own the trousers? Rent the blazer.</p><div class="detail-accordions"><details open><summary>DETAILS</summary><p>Prototype capsule piece: ${item.style.join(', ')} styling with a considered formal cut.</p></details><details><summary>FIT</summary><p>Choose your usual size. Between sizes? Choose the larger option for this prototype.</p></details><details><summary>CONDITION</summary><p>Excellent.</p></details><details><summary>CLEANING</summary><p>Professionally cleaned after every rental.</p></details><details><summary>DELIVERY & RETURNS</summary><p>We recommend receiving pieces two days before your event and returning one day after.</p></details><details><summary>RENTAL POLICY</summary><p>Prototype terms only. No actual payment or fulfilment is processed.</p></details></div></section></div><section class="also-like section"><p class="eyebrow">YOU MAY ALSO LIKE</p><div class="featured-grid">${inventory.filter(x=>x.id!==item.id&&x.collection===item.collection).slice(0,4).map(productCard).join('')}</div></section>`;let size=item.sizes[0];qsa('[data-size]').forEach(b=>b.onclick=()=>{size=b.dataset.size;qsa('[data-size]').forEach(x=>x.classList.toggle('active',x===b));});qs('#productDate').onchange=e=>{const win=rentalWindow(e.target.value);qs('#datePreview').textContent=win?`Receive: ${win.receive} · Return: ${win.return}`:'Choose an event date for your delivery window.';};qs('#addProduct').onclick=()=>addToBag(item.id,size,qs('#productDate').value);qs('#saveProduct').onclick=()=>{toggleSaved(item.id);qs('#saveProduct').textContent=getSaved().includes(item.id)?'Saved item':'Save item';};bindProductCards(root);});
